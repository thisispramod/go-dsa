1. Arrays & Strings (Warm-up, but scrutinized closely)

✅ First Non-Repeating Character (already solved!)
✅ Two Sum (already solved!)
✅ Reverse a string / array in-place
Find duplicate elements in an array
Maximum subarray sum (Kadane's Algorithm)
Move zeroes to end (in-place)
Check if two strings are anagrams

2. Hashmap-based

Group anagrams together
Find majority element (appears more than n/2 times)
✅Longest substring without repeating characters
Subarray sum equals K

3. Stack-based

✅ Valid Parentheses (already solved!)
Next Greater Element
Min Stack (design a stack that also returns min in O(1))
Evaluate Reverse Polish Notation (calculator-style — good for Go interviews since it tests parsing logic)

4. Linked List

Reverse a linked list (iterative aur recursive dono pooch sakte hain)
Detect cycle in a linked list (Floyd's algorithm — "tortoise and hare")
Merge two sorted linked lists
Find middle of linked list

5. Two Pointers / Sliding Window

Container with most water
Longest substring with K distinct characters
Find pair with given sum in sorted array

6. Trees (kam common at 4yr backend level, but basic level pooch sakte hain)

Level order traversal (BFS) — Go mein ye queue (slice se simulate) test karta hai
Check if binary tree is balanced
Maximum depth of binary tree

7. Go-Specific "DSA meets Concurrency" (ye especially aapke case mein highly likely hai!)

✅ Worker Pool Pattern (already covered!)
Producer-Consumer using channels
Rate limiter implementation using channels/tickers
Concurrent-safe counter/cache (Mutex ya RWMutex ke saath)


Hashmap-based problems (Two Sum type — already strong ✅)
Stack-based (already strong ✅)
String manipulation (parsing, validation — healthcare data often needs strict validation)
Concurrency + DSA combo (worker pool, rate limiter) — ye sabse zyada differentiate karega aapko fresher-level candidates se